import '../CSS/Info.css';
const Info = () => {
    return (
        <div className={"info-container"}>
            <h1 className={"info-title"}>Weather application</h1>
            <p className={"info-description"}>Your city weather:</p>
        </div>
    );
}

export default Info;